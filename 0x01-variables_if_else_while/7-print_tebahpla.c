int main(void)
{
        char low;
	char low;

        for (low = 'z'; low >= 'a'; low--)
                putchar(low);
        putchar('\n');
	for (low = 'z'; low >= 'a'; low--)
		putchar(low);
		putchar('\n');

        return (0);
	return (0);
}

